# dotfiles
personal stuff
using : swaync,
        swayosd,
        kitty (zsh),
        waybar,
        swww (nsxiv as wallpaper selector) ,
        pywal,
        firefox (pywal extension),
        termusic,
        vscodium,
        rofi,
        hyprlock.

please check the scripts one by one before applying, because this is tailored for my laptop. some colorscheme may need to be generated from pywal (kitty), also rofi uses wallpaper that i symlink to my current wallpaper from the output of wallpaperselector.
